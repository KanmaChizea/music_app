import 'package:flutter/material.dart';

class PlaylistMobile extends StatelessWidget {
  const PlaylistMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Playlst bitvhhhhh'),
    );
  }
}
